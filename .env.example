# Copy this file to .env and adjust values for your environment.
# The .env file is gitignored — never commit credentials.

# PostgreSQL connection string used by load.py
POSTGRES_CONN=postgresql://airflow:airflow@localhost:5432/adventureworks
